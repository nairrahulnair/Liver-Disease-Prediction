# Liver-Disease-Prediction
# With around 20 independent variables(a mix of ordinal and continous data type) I chose various Esemble techniques for predictions
# Other classification models like Logistic and SVM were given a shot but Random Forest returned the best F1 score(weighted).
